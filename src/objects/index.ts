// Object and Types alias

type Point = { x: number; y: number };

// using types alias
let center: Point = { x: 0, y: 0 };

let unit: Point = { x: 1, y: 1 };

//adavantages of using type alias
// 1. type alias can be used to create a type that is more readable
// 2. type alias can be used to create a type that is more flexible
