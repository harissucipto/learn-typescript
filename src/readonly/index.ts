type Point2 = {
  readonly x: number;
  y: number;
};

const point: Point2 = {
  x: 0,
  y: 0,
};

// Variable assignment is not allowed
// point = { x: 1, y: 1 };

// Property assignment is not allowed
// point.x = 1;
